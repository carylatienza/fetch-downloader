## 1. UI Redesign & CSS Fixes

- [x] 1.1 Fix `.sidebar` CSS rule in `src/app/contact/page.module.css` to `flex-direction: column`.
- [x] 1.2 Update `src/app/contact/page.module.css` with 2-column input row styles (`.rowInputs`) and responsive breakpoints.
- [x] 1.3 Update `src/app/contact/page.js` to add input row wrapper, icon accents for labels, and improved card layout.

## 2. Verification

- [x] 2.1 Verify layout with `npm run build` and visual inspection.
