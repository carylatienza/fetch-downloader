## 1. Preview Card Image Referrer Policy & Error Fallback

- [x] 1.1 Add `referrerPolicy="no-referrer"` attribute to `<img />` tag in `src/components/PreviewCard/PreviewCard.js`
- [x] 1.2 Add `imageError` state handler (`onError`) to fallback gracefully to icon placeholder if image load fails
- [x] 1.3 Verify thumbnail rendering across Facebook, Instagram, YouTube, and X URLs end-to-end
