## 1. Extractor Multi-Photo Collection

- [x] 1.1 Update `FacebookExtractor` in `src/lib/extractors/facebook.js` to collect all photo items into `images: [...]` array

## 2. Preview Card Carousel Navigation Controls

- [x] 2.1 Update `src/components/PreviewCard/PreviewCard.js` to render `<` and `>` overlay navigation buttons and `Photo X of Y` counter badge inside thumbnail container
- [x] 2.2 Verify multi-photo carousel navigation and single/ZIP download triggers end-to-end
