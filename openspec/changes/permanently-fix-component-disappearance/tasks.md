## 1. Scroll Observer Provider Upgrade

- [x] 1.1 Upgrade `ScrollObserverProvider.js` with `MutationObserver` and instant bounding client rect viewport check.
- [x] 1.2 Add CSS fallback rules in `globals.css` for `.reveal` elements.

## 2. Hero Spacing & Layout Alignment

- [x] 2.1 Refine `heroSection` padding and `stepsSection` spacing in `page.module.css` to prevent header overlap.

## 3. Verification

- [x] 3.1 Test home page rendering across re-renders and verify with `npm run build`.
