## 1. Mount Fixed Top Atmosphere Spotlight

- [x] 1.1 Mount `<div className="bg-spotlight" aria-hidden="true" />` in `src/app/layout.js` inside `<body>`
- [x] 1.2 Verify build with `npm run build` and capture final screenshots/recordings with browser subagent
