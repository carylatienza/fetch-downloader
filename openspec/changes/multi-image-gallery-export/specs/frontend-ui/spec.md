## MODIFIED Requirements

### Requirement: Media preview card
The system SHALL display an interactive gallery carousel when multiple images are extracted, providing photo navigation and batch `.zip` download capabilities.

#### Scenario: Multi-image gallery carousel rendering
- **WHEN** extracted media contains multiple images
- **THEN** preview card displays photo carousel navigation (`Previous`, `Next`, image counter), individual photo download trigger, and "Download All (.zip)" batch trigger
